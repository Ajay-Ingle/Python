"""
just add the mulitprocessing.Lock()

and two functions:
    lock.acquire() - for adding lock so no other process can acess the resource
    lock.release()- for releasing the lock so the resource can be used by other process
"""